export enum pageRotes {
  main = "/",
  login = "/login",
  register = "/register",
  clients = "/clients",
  tickets = "/tickets",
  coaches = "/coaches",
  gym = "/gym/:id",
  coach = "/coach/:id",
  workouts = "/workouts"
}
